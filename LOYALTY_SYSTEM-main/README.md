# CentralPerk

CentralPerk is a loyalty rewards web app built with Next.js, React, and Supabase.

## Run locally

```bash
npm install
npm run dev
```

The main app lives in the `CentralPerk/` directory.
